version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: devlink-mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=devlink
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
